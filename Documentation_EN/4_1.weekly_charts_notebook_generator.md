# 🎵 Script 4_1: Weekly Music Charts Notebook Generator

![MIT License](https://img.shields.io/badge/license-MIT-9ecae1?style=flat-square&logo=open-source-initiative&logoColor=white) ![Notebook Generation](https://img.shields.io/badge/Notebook-Generation-blue?style=flat-square) ![AI Insights](https://img.shields.io/badge/AI-Insights-purple?style=flat-square)

![Python](https://img.shields.io/badge/Python-3776AB?style=flat-square&logo=python&logoColor=white) ![Pandas](https://img.shields.io/badge/Pandas-150458?style=flat-square&logo=pandas&logoColor=white) ![Matplotlib](https://img.shields.io/badge/Matplotlib-11557c?style=flat-square&logo=python&logoColor=white) ![Seaborn](https://img.shields.io/badge/Seaborn-3776AB?style=flat-square&logo=python&logoColor=white) ![Squarify](https://img.shields.io/badge/Squarify-FF6F61?style=flat-square&logo=python&logoColor=white) ![Jupyter](https://img.shields.io/badge/Jupyter-F37626?style=flat-square&logo=jupyter&logoColor=white) ![DeepSeek](https://custom-icon-badges.demolab.com/badge/DeepSeek-4D6BFF?logo=deepseek&logoColor=white&style=flat-square)

## 📥 Quick Downloads
| Document                | Format                                                     |                                                    
| ------------------------- | ------------------------------------------------------------ |
| **🇬🇧 English Documentation** | [PDF](https://drive.google.com/file/d/XXXXXXXXXXXXXXXXXX/view?usp=drive_link) |
| **🇪🇸 Spanish Documentation**  | [PDF](https://drive.google.com/file/d/XXXXXXXXXXXXXXXXXX/view?usp=sharing) |

## 📋 General Description

This project is the fourth component of the YouTube Charts intelligence system. It takes the enriched weekly chart database (from Script 3) and **automatically generates professional Jupyter notebooks** with comprehensive visual analysis and AI-generated insights in both English and Spanish.

The system produces two fully-executed notebooks per week containing **25+ visualizations**, **12 analysis sections**, and **AI-powered commentary** generated by DeepSeek API, all cached to avoid redundant API calls.

### Key Features

- **25+ Visualizations**: Static charts using matplotlib, seaborn, and squarify (no JavaScript required)
- **AI-Generated Insights**: DeepSeek API provides intelligent commentary for each analysis section
- **Bilingual Output**: Generates both English and Spanish notebooks from a single run
- **Smart Caching**: Per-week, per-language cache system avoids redundant API calls
- **Weekly Automation**: Fully automated via GitHub Actions, runs every Monday
- **Sliding Window Retention**: Keeps only last 6 notebooks per language automatically
- **Executive Summary**: 30-line detailed summary with actionable recommendations
- **YouTube-Style Styling**: Visualizations use YouTube's color palette (#FF0000, #F9F9F9, etc.)
- **Zero JavaScript**: All charts are static matplotlib/seaborn, compatible with GitHub preview
- **Database Agnostic**: Works with any enriched database from Script 3

## 📊 Analysis Sections

| Section | Content | Visualizations |
|:--------|:--------|:---------------|
| **1. Introduction** | AI-generated weekly overview | None |
| **2. Setup** | Environment configuration | None |
| **3. Data Preview** | Sample of loaded data | Table |
| **4. General Statistics** | Key metrics (songs, countries, genres, views, likes) | Summary table |
| **5. Country Analysis** | Geographic distribution | Pie chart, bar charts, per-country top 5 |
| **6. Genre Analysis** | Genre distribution and engagement | Treemap, bar chart, heatmap |
| **7. Song Metrics** | Top songs by views, likes, engagement | Tables |
| **8. Video Metrics** | Performance by video type (official, lyric, live) | Bar charts |
| **9. Temporal Analysis** | Trends by release quarter | Line charts, bar chart |
| **10. Collaborations** | Solo vs collaboration performance | Scatter plots, summary table |
| **11. Executive Summary** | 30-line AI-generated strategic summary | None |
| **12. Attribution** | Metadata and sources | Table |

## 🔄 Process Flow Diagram

### **Legend**
| Color        | Type     | Description                   |
| :----------- | :------- | :---------------------------- |
| 🔵 Blue       | Input    | Source data (enriched database) |
| 🟠 Orange     | Process  | Internal processing logic     |
| 🟣 Purple     | API      | DeepSeek AI service           |
| 🟢 Green      | Cache    | JSON cache per week/language  |
| 🔴 Red        | Decision | Conditional branching points  |
| 🟢 Dark Green | Output   | Generated notebooks           |

### **Diagram 1: Main Flow Overview**

<img src="https://drive.google.com/uc?export=view&id=XXXXXXXXXXXXXXXXXX" alt="Main Flow Overview" width="400">

This diagram shows the **high-level pipeline** of the entire system:

1. **Input**: Reads enriched weekly database (`youtube_charts_YYYY-WXX_enriched.db`)
2. **Data Loading**: Loads data, calculates engagement, extracts temporal features
3. **Data Summaries**: Pre-calculates metrics for AI prompts (top countries, genre engagement, etc.)
4. **Cache Check**: For each section, checks if insight exists in cache
   - **If cached** → Uses cached insight ✅
   - **If not cached** → Queries DeepSeek API → Saves to cache
5. **Notebook Generation**: Creates Jupyter notebook with code cells and AI insights
6. **Notebook Execution**: Runs `nbconvert --execute` to embed all outputs
7. **Cleanup**: Removes old notebooks (keeps last 6 per language)
8. **GitHub Commit**: Automatically commits and pushes changes

### **Diagram 2: AI Insights Caching System**

<img src="https://drive.google.com/uc?export=view&id=XXXXXXXXXXXXXXXXXX" alt="Caching System" width="350">

This diagram details the **intelligent caching mechanism**:

1. **Section Request**: System needs insight for a specific section (e.g., "genre_engagement")
2. **Data Fingerprint**: Generates MD5 hash of relevant data subset:
   - `general_stats` → hash of (total_songs, total_views, total_likes)
   - `top_countries` → hash of top 10 countries string
   - `genre_engagement` → hash of genre engagement rates
3. **Cache Lookup**: Checks JSON file for key `{section}_{hash}`
   - **Cache hit** → Returns insight immediately (no API call)
   - **Cache miss** → Proceeds to DeepSeek API
4. **DeepSeek Query**: Sends language-specific prompt with data summary
5. **Insight Storage**: Saves result with hash key for future runs
6. **Cache Isolation**: Separate cache files per week and language
   - `youtube_charts_2026-W14_en.json`
   - `youtube_charts_2026-W14_es.json`

### **Diagram 3: Notebook Generation Pipeline**

<img src="https://drive.google.com/uc?export=view&id=XXXXXXXXXXXXXXXXXX" alt="Notebook Generation" width="350">

This diagram shows how the **Jupyter notebook is constructed**:

1. **Template Loading**: Section titles and structure defined in `get_section_titles()`
2. **Cell Creation**: For each section, creates markdown and code cells:
   - **Markdown cells**: Section headers and AI insights
   - **Code cells**: Python code for analysis and visualizations
3. **Dynamic Content Injection**:
   - Database path (relative path from notebook to repo root)
   - Week/year information
   - AI insights (from cache or API)
4. **nbformat Assembly**: Builds notebook using `nbformat.v4` structures
5. **File Writing**: Saves as `.ipynb` with proper metadata (Python 3 kernel)

### **Diagram 4: Visualization Suite**

<img src="https://drive.google.com/uc?export=view&id=XXXXXXXXXXXXXXXXXX" alt="Visualizations" width="400">

The notebook includes **25+ static visualizations**:

| Type | Count | Examples |
|:-----|:------|:---------|
| **Bar Charts (horizontal)** | 5 | Top countries by songs, top countries by likes, engagement by genre, channel types |
| **Bar Charts (vertical)** | 3 | Release distribution, video type views, video type engagement |
| **Line Charts** | 2 | Views evolution by quarter, engagement evolution by quarter |
| **Pie Charts** | 1 | Continent distribution |
| **Treemap** | 1 | Genre distribution (using squarify) |
| **Heatmaps** | 1 | Country vs genre matrix |
| **Histograms** | 1 | Duration distribution with KDE |
| **Box Plots** | 1 | Duration statistics |
| **Scatter Plots** | 2 | Views vs artist count, engagement vs artist count |
| **Tables** | 8 | Statistics, top songs, video metrics, etc. |

All visualizations use **YouTube-inspired colors**:
- Primary: `#FF0000` (YouTube red)
- Background: `#FFFFFF` and `#F9F9F9`
- Text: `#0F0F0F` and `#606060`
- Grid: `#E5E5E5`

---

## 🔍 Detailed Analysis of `4_1.weekly_charts_notebook_generator.py`

### Code Structure

#### **1. Configuration Class**

```python
class Config:
    """Configuration settings for the notebook generator."""
    
    # GitHub repository
    REPO_OWNER = "adroguetth"
    REPO_NAME = "Music-Charts-Intelligence"
    FOLDER_PATH = "charts_archive/3_enrich-chart-data"
    
    # Output paths
    OUTPUT_EN_PATH = Path("Notebook_EN/weekly")
    OUTPUT_ES_PATH = Path("Notebook_ES/weekly")
    CACHE_EN_PATH = Path("Notebook_EN/weekly/cache")
    CACHE_ES_PATH = Path("Notebook_ES/weekly/cache")
    
    # DeepSeek API
    DEEPSEEK_API_URL = "https://api.deepseek.com/v1/chat/completions"
#### **2. AI Insights Cache System**

```python
class AIInsightsCache:
    """
    Cache manager for AI-generated insights.
    
    Features:
    - Isolated by week and language
    - Data fingerprint-based cache keys
    - JSON persistence
    """
    
    def _get_data_hash(self, df: pd.DataFrame, section: str) -> str:
        """Generate MD5 hash of relevant data subset."""
        if section == 'general_stats':
            key_data = f"{len(df)}_{df['views'].sum()}_{df['likes'].sum()}"
        elif section == 'top_countries':
            key_data = df['artist_country'].value_counts().head(10).to_string()
        # ... section-specific hashing
        return hashlib.md5(key_data.encode()).hexdigest()
```

**Cache file structure:**

```json
{
  "introduction_a1b2c3d4e5f6": "This week's YouTube charts show...",
  "general_stats_f6e5d4c3b2a1": "The dataset contains 100 songs from...",
  "top_countries_1a2b3c4d5e6f": "United States dominates with 45 songs..."
}
```

#### **3. DeepSeek API Integration**

```python
def get_ai_insight(section: str, data_summary: Dict, df: pd.DataFrame, 
                   language: str, week: str) -> str:
    """
    Query DeepSeek API for section insights.
    
    - Language-specific prompts (English/Spanish)
    - Max tokens: 600 for regular sections, 2000 for introduction/executive summary
    - Temperature: 0.7 (balanced creativity/consistency)
    - Rate limiting: Handled by GitHub Actions execution time
    """
```

**Prompt examples for Spanish notebook:**

```python
# Introduction prompt (Spanish)
prompt = f"""
Escribe una introducción atractiva y personalizada para un informe semanal 
de charts musicales de YouTube, basada en estos datos:

- Total canciones analizadas: {total_songs}
- Países representados: {unique_countries}
- Total de vistas: {total_views:,}

La introducción debe ser persuasiva, destacar la riqueza de los datos,
y tener una extensión de 6-8 líneas en español.
"""
```

#### **4. Database Operations**

```python
def load_data(db_path: Path) -> pd.DataFrame:
    """Load and prepare data from SQLite database."""
    conn = sqlite3.connect(db_path)
    df = pd.read_sql_query("SELECT * FROM enriched_songs", conn)
    conn.close()
    
    # Feature engineering
    df['upload_date'] = pd.to_datetime(df['upload_date'], errors='coerce')
    df['upload_quarter'] = df['upload_date'].dt.quarter
    df['engagement'] = np.where(df['views'] > 0, 
                                 (df['likes'] / df['views'] * 100).round(2), 
                                 0.0)
    return df
```

#### **5. Notebook Generation with nbformat**

```python
def generate_notebook(df: pd.DataFrame, db_info: Tuple, 
                      insights: Dict[str, str], output_path: Path, 
                      language: str) -> None:
    """
    Generate Jupyter notebook using nbformat.
    
    Process:
    1. Create empty notebook with new_notebook()
    2. Set kernel metadata (Python 3)
    3. Add title cell with week information
    4. For each section:
       - Add markdown cell with section header
       - Add markdown cell with AI insight (if available)
       - Add code cell with analysis/visualization code
    5. Write notebook to file
    """
```

**Example of a generated code cell:**

```python
# Top Countries by Song Count (barh)
top_countries = (df
    .groupby('artist_country')
    .agg(total_songs=('rank', 'count'))
    .reset_index()
    .sort_values('total_songs', ascending=False)
    .head(10))

fig, ax = plt.subplots(figsize=(10, 6))
colors = plt.cm.Reds(np.linspace(0.4, 1, len(top_countries)))[::-1]
bars = ax.barh(top_countries['artist_country'], top_countries['total_songs'],
               color=colors, edgecolor='none', height=0.65)
# ... styling
plt.show()
```

#### **6. Data Summaries for AI Prompts**

```python
def get_data_summaries(df: pd.DataFrame) -> Dict[str, Dict]:
    """
    Pre-calculate all data summaries needed for AI insights.
    
    Returns nested dictionary with metrics for each section:
    - introduction: total_songs, unique_countries, total_views, etc.
    - top_countries: formatted string of top 10 countries
    - genre_engagement: engagement rates by genre
    - video_metrics: official/lyric/live performance stats
    - collaborations: solo vs collab comparison
    - executive_summary: aggregated key findings
    """
```

#### **7. CLI Argument Parsing**

```python
parser = argparse.ArgumentParser(
    description='Generate weekly music charts analysis notebooks with AI insights'
)
parser.add_argument('--week', type=str, 
                   help='Week to process (format: YYYY-WXX)')
parser.add_argument('--language', type=str, 
                   choices=['en', 'es', 'both'], default='both')
parser.add_argument('--db-path', type=str,
                   help='Path to specific database file')
```

### **Output File Structure**

```text
Notebook_EN/weekly/
├── youtube_charts_2026-W14.ipynb    # Executed English notebook
├── youtube_charts_2026-W13.ipynb
├── youtube_charts_2026-W12.ipynb
├── cache/
│   ├── youtube_charts_2026-W14_en.json
│   ├── youtube_charts_2026-W13_en.json
│   └── ...

Notebook_ES/weekly/
├── youtube_charts_2026-W14.ipynb    # Executed Spanish notebook
├── youtube_charts_2026-W13.ipynb
├── youtube_charts_2026-W12.ipynb
├── cache/
│   ├── youtube_charts_2026-W14_es.json
│   ├── youtube_charts_2026-W13_es.json
│   └── ...
```


## ⚙️ GitHub Actions Workflow Analysis (`4-generate-weekly-notebooks.yml`)

### **Workflow Structure**

```yaml
name: 4.1- Weekly Music Charts Notebook Generator

on:
  schedule:
    - cron: '0 15 * * 1'
  workflow_dispatch:
    inputs:
      week:
        description: 'Specific week to process (format: YYYY-WXX)'
        required: false
        type: string
      language:
        description: 'Language to generate'
        required: false
        default: 'both'
        type: choice
        options:
          - both
          - en
          - es

permissions:
  contents: write
```

### **Job Steps**

#### **Step 1: 📥 Checkout Repository**

```yaml
- name: 📥 Checkout repository
  uses: actions/checkout@v4
  with:
    fetch-depth: 0  # Full history for git operations
```

#### **Step 2: 🐍 Setup Python**

```yaml
- name: 🐍 Setup Python
  uses: actions/setup-python@v5
  with:
    python-version: '3.12'
    cache: 'pip'  # Dependency caching for faster runs
```

#### **Step 3: 📦 Install Python Dependencies**

```python
- name: 📦 Install Python dependencies
  run: |
    pip install --upgrade pip
    # Core data science libraries
    pip install pandas numpy matplotlib seaborn scipy requests
    # Treemap support (replaces plotly for static visualizations)
    pip install squarify
    # Jupyter notebook generation and execution
    pip install jupyter nbformat nbconvert
```

**Dependencies rationale:**

- `pandas`, `numpy`: Data manipulation
- `matplotlib`, `seaborn`: Static visualizations
- `squarify`: Static treemap (replaces plotly)
- `scipy`: KDE for duration analysis
- `jupyter`, `nbformat`, `nbconvert`: Notebook generation and execution
- `requests`: DeepSeek API calls

#### **Step 4: 🔍 Determine Week to Process**

```yaml
- name: 🔍 Determine week to process
  id: get_week
  run: |
    if [ -n "${{ github.event.inputs.week }}" ]; then
      echo "week=${{ github.event.inputs.week }}" >> $GITHUB_OUTPUT
      echo "language=${{ github.event.inputs.language }}" >> $GITHUB_OUTPUT
    else
      LATEST_DB=$(ls -1 charts_archive/3_enrich-chart-data/youtube_charts_*.db 2>/dev/null | sort -r | head -1)
      if [ -z "$LATEST_DB" ]; then
        echo "❌ No database found in charts_archive"
        exit 1
      fi
      WEEK=$(basename "$LATEST_DB" | sed 's/youtube_charts_\(.*\)_enriched.db/\1/')
      echo "week=$WEEK" >> $GITHUB_OUTPUT
      echo "language=both" >> $GITHUB_OUTPUT
    fi
```

#### **Step 5: 🏗️ Generate Notebooks (Code Only)**

```yaml
- name: 🏗️ Generate notebooks (code only)
  run: |
    python scripts/4_1.weekly_charts_notebook_generator.py \
      --week ${{ steps.get_week.outputs.week }} \
      --language ${{ steps.get_week.outputs.language }}
```

#### **Step 6: ⚡ Execute Notebooks to Embed Outputs**

```yaml
- name: ⚡ Execute notebooks to embed outputs
  run: |
    cd ${{ github.workspace }}
    
    # Execute English notebook
    EN_NOTEBOOK="Notebook_EN/weekly/youtube_charts_${{ steps.get_week.outputs.week }}.ipynb"
    if [ -f "$EN_NOTEBOOK" ]; then
      echo "🇬🇧 Executing English notebook..."
      jupyter nbconvert --to notebook --execute \
        "$EN_NOTEBOOK" \
        --output "$(basename "$EN_NOTEBOOK")" \
        --output-dir Notebook_EN/weekly/
    fi
    
    # Execute Spanish notebook
    ES_NOTEBOOK="Notebook_ES/weekly/youtube_charts_${{ steps.get_week.outputs.week }}.ipynb"
    if [ -f "$ES_NOTEBOOK" ]; then
      echo "🇪🇸 Executing Spanish notebook..."
      jupyter nbconvert --to notebook --execute \
        "$ES_NOTEBOOK" \
        --output "$(basename "$ES_NOTEBOOK")" \
        --output-dir Notebook_ES/weekly/
    fi
```

#### **Step 7: 🧹 Keep Only Last 6 Notebooks Locally**

```yaml
- name: 🧹 Keep only last 6 notebooks locally
  run: |
    MAX_KEEP=6
    KEEP_PLUS_ONE=$((MAX_KEEP + 1))
    
    # English notebooks
    cd Notebook_EN/weekly
    ls -1 *.ipynb 2>/dev/null | sort -r | tail -n +${KEEP_PLUS_ONE} | xargs -r rm -f
    cd ../..
    
    # Spanish notebooks
    cd Notebook_ES/weekly
    ls -1 *.ipynb 2>/dev/null | sort -r | tail -n +${KEEP_PLUS_ONE} | xargs -r rm -f
    cd ../..
    
    # Cache files (keep last 6 per language)
    mkdir -p Notebook_EN/weekly/cache
    cd Notebook_EN/weekly/cache
    ls -1 *.json 2>/dev/null | sort -r | tail -n +${KEEP_PLUS_ONE} | xargs -r rm -f
    cd ../../../..
    
    mkdir -p Notebook_ES/weekly/cache
    cd Notebook_ES/weekly/cache
    ls -1 *.json 2>/dev/null | sort -r | tail -n +${KEEP_PLUS_ONE} | xargs -r rm -f
    cd ../../../..
    
    echo "🧹 Cleaned up: Keeping only last ${MAX_KEEP} notebooks per language"
```

**liding Window Behavior:**

| Execution | Notebooks | Action                               |
| :-------- | :-------- | :----------------------------------- |
| 1st-6th   | 1-6       | None (below limit)                   |
| 7th       | 7         | Deletes oldest (keeps 6 most recent) |
| 8th       | 7         | Deletes oldest (keeps 6 most recent) |

#### **Step 8: 💾 Commit and Push Changes**

```yaml
- name: 💾 Commit and push changes
  run: |
    git config user.name "github-actions[bot]"
    git config user.email "github-actions[bot]@users.noreply.github.com"
    
    git add Notebook_EN/weekly/ Notebook_ES/weekly/
    
    if git diff --staged --quiet; then
      echo "📭 No changes to commit"
    else
      git commit -m "📊 Weekly charts analysis - ${{ steps.get_week.outputs.week }} [skip ci]"
      git push
    fi
```

#### **Step 9: 📊 Summary**

```yaml
- name: 📊 Summary
  run: |
    echo "=========================================="
    echo "🎉 Weekly Charts Analysis Complete! 🎉"
    echo "=========================================="
    echo "📅 Week: ${{ steps.get_week.outputs.week }}"
    echo "🌐 Language: ${{ steps.get_week.outputs.language }}"
    echo ""
    echo "📁 Generated files (with outputs):"
    echo "  🇬🇧 Notebooks in Notebook_EN/weekly/"
    echo "  🇪🇸 Notebooks in Notebook_ES/weekly/"
    echo "  🗑️ Only last 6 notebooks retained locally"
    echo "=========================================="
```

### **Cron Scheduling**

```crom
'0 15 * * 1'  # Minute 0, Hour 15, Any day of month, Any month, Monday
```

### **Execution Flow Timeline (Mondays)**

```text
12:00 UTC ─→ Script 1: Download charts
    ↓
13:00 UTC ─→ Script 2: Artist enrichment
    ↓
14:00 UTC ─→ Script 3: Chart enrichment
    ↓
15:00 UTC ─→ Script 4_1: Notebook generation (THIS SCRIPT)
    ↓
15:10 UTC ─→ Notebooks executed and committed
```

---

## 🔐 Required Secrets

| Secret             | Purpose                                                      |
| :----------------- | :----------------------------------------------------------- |
| `DEEPSEEK_API_KEY` | Used by DeepSeek API to generate AI insights for each section. Required for full functionality; without it, insights show "AI insights not available (API key not configured)". |
