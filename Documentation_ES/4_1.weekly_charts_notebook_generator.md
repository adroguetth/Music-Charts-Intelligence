# 🎵 Script 4_1: Generador de Notebooks Semanales de Charts Musicales

![MIT License](https://img.shields.io/badge/license-MIT-9ecae1?style=flat-square&logo=open-source-initiative&logoColor=white) ![Generación de Notebooks](https://img.shields.io/badge/Notebooks-Generación-blue?style=flat-square) ![Insights IA](https://img.shields.io/badge/IA-Insights-purple?style=flat-square)

![Python](https://img.shields.io/badge/Python-3776AB?style=flat-square&logo=python&logoColor=white) ![Pandas](https://img.shields.io/badge/Pandas-150458?style=flat-square&logo=pandas&logoColor=white) ![Matplotlib](https://img.shields.io/badge/Matplotlib-11557c?style=flat-square&logo=python&logoColor=white) ![Seaborn](https://img.shields.io/badge/Seaborn-3776AB?style=flat-square&logo=python&logoColor=white) ![Squarify](https://img.shields.io/badge/Squarify-FF6F61?style=flat-square&logo=python&logoColor=white) ![Jupyter](https://img.shields.io/badge/Jupyter-F37626?style=flat-square&logo=jupyter&logoColor=white) ![DeepSeek](https://custom-icon-badges.demolab.com/badge/DeepSeek-4D6BFF?logo=deepseek&logoColor=white&style=flat-square)

## 📥 Descargas Rápidas
| Documento | Formato |
| ------------------------- | ------------------------------------------------------------ |
| **🇬🇧 Documentación en Inglés** | [PDF](https://drive.google.com/file/d/XXXXXXXXXXXXXXXXXX/view?usp=drive_link) |
| **🇪🇸 Documentación en Español** | [PDF](https://drive.google.com/file/d/XXXXXXXXXXXXXXXXXX/view?usp=sharing) |

## 📋 Descripción General

Este proyecto es el cuarto componente del sistema de inteligencia de charts de YouTube. Toma la base de datos semanal enriquecida (del Script 3) y **genera automáticamente notebooks profesionales de Jupyter** con análisis visual completo e insights generados por IA tanto en inglés como en español.

El sistema produce dos notebooks completamente ejecutados por semana que contienen **más de 25 visualizaciones**, **12 secciones de análisis** y **comentarios generados por IA** mediante la API de DeepSeek, todo almacenado en caché para evitar llamadas redundantes a la API.

### Características Principales

- **📊 Más de 25 Visualizaciones**: Gráficos estáticos usando matplotlib, seaborn y squarify (sin necesidad de JavaScript)
- **🤖 Insights Generados por IA**: La API de DeepSeek proporciona comentarios inteligentes para cada sección de análisis
- **🌐 Salida Bilingüe**: Genera notebooks en inglés y español desde una sola ejecución
- **💾 Caché Inteligente**: Sistema de caché por semana e idioma evita llamadas redundantes a la API
- **📅 Automatización Semanal**: Completamente automatizado vía GitHub Actions, se ejecuta cada lunes
- **🗂️ Retención de Ventana Deslizante**: Mantiene solo los últimos 6 notebooks por idioma automáticamente
- **📈 Resumen Ejecutivo**: Resumen detallado de 30 líneas con recomendaciones accionables
- **🎨 Estilo YouTube**: Las visualizaciones usan la paleta de colores de YouTube (#FF0000, #F9F9F9, etc.)
- **🔧 Cero JavaScript**: Todos los gráficos son estáticos (matplotlib/seaborn), compatibles con vista previa de GitHub
- **💾 Agnóstico de Base de Datos**: Funciona con cualquier base de datos enriquecida del Script 3

## 📊 Secciones de Análisis

| Sección | Contenido | Visualizaciones |
|:--------|:----------|:----------------|
| **1. Introducción** | Resumen semanal generado por IA | Ninguna |
| **2. Configuración** | Configuración del entorno | Ninguna |
| **3. Vista Previa** | Muestra de datos cargados | Tabla |
| **4. Estadísticas Generales** | Métricas clave (canciones, países, géneros, vistas, likes) | Tabla resumen |
| **5. Análisis por País** | Distribución geográfica | Gráfico circular, gráficos de barras, top 5 por país |
| **6. Análisis por Género** | Distribución y engagement por género | Treemap, gráfico de barras, mapa de calor |
| **7. Métricas de Canciones** | Top canciones por vistas, likes, engagement | Tablas |
| **8. Métricas de Video** | Rendimiento por tipo de video (oficial, lyric, live) | Gráficos de barras |
| **9. Análisis Temporal** | Tendencias por trimestre de lanzamiento | Gráficos de líneas, gráfico de barras |
| **10. Colaboraciones** | Rendimiento solista vs colaboraciones | Diagramas de dispersión, tabla resumen |
| **11. Resumen Ejecutivo** | Resumen estratégico de 30 líneas generado por IA | Ninguna |
| **12. Atribución** | Metadatos y fuentes | Tabla |

## 🔄 Diagrama de Flujo del Proceso

### **Leyenda**
| Color | Tipo | Descripción |
| :----------- | :------- | :---------------------------- |
| 🔵 Azul | Entrada | Datos fuente (base de datos enriquecida) |
| 🟠 Naranja | Proceso | Lógica de procesamiento interno |
| 🟣 Púrpura | API | Servicio de IA DeepSeek |
| 🟢 Verde | Caché | Caché JSON por semana/idioma |
| 🔴 Rojo | Decisión | Puntos de ramificación condicional |
| 🟢 Verde Oscuro | Salida | Notebooks generados |

### **Diagrama 1: Vista General del Flujo Principal**

<img src="https://raw.githubusercontent.com/adroguetth/Music-Charts-Intelligence/main/Documentation_ES/Diagramas/4_1.weekly_charts_notebook_generator/ES-Diagram%201.png" alt="Diagrama 1: Vista General del Flujo Principal" width="600">

Este diagrama muestra el **pipeline de alto nivel** de todo el sistema:

1. **Entrada**: Lee la base de datos semanal enriquecida (`youtube_charts_YYYY-WXX_enriched.db`)
2. **Carga de Datos**: Carga datos, calcula engagement, extrae características temporales
3. **Resúmenes de Datos**: Precalcula métricas para prompts de IA (top países, engagement por género, etc.)
4. **Verificación de Caché**: Para cada sección, verifica si el insight existe en caché
   - **Si está en caché** → Usa insight almacenado ✅
   - **Si no está en caché** → Consulta API de DeepSeek → Guarda en caché
5. **Generación de Notebook**: Crea notebook Jupyter con celdas de código e insights de IA
6. **Ejecución de Notebook**: Ejecuta `nbconvert --execute` para incrustar todas las salidas
7. **Limpieza**: Elimina notebooks antiguos (mantiene últimos 6 por idioma)
8. **Commit a GitHub**: Automáticamente commitea y sube los cambios

### **Diagrama 2: Sistema de Caché de Insights de IA**

<img src="https://raw.githubusercontent.com/adroguetth/Music-Charts-Intelligence/main/Documentation_ES/Diagramas/4_1.weekly_charts_notebook_generator/ES-Diagram%202.png" alt="Diagrama 2: Sistema de Caché de Insights de IA" width="600">

Este diagrama detalla el **mecanismo de caché inteligente**:

1. **Solicitud de Sección**: El sistema necesita un insight para una sección específica (ej., "genre_engagement")
2. **Huella Digital de Datos**: Genera hash MD5 del subconjunto de datos relevante:
   - `general_stats` → hash de (total_canciones, total_vistas, total_likes)
   - `top_countries` → hash del string de top 10 países
   - `genre_engagement` → hash de tasas de engagement por género
3. **Búsqueda en Caché**: Verifica archivo JSON con clave `{sección}_{hash}`
   - **Acierto de caché** → Retorna insight inmediatamente (sin llamada API)
   - **Fallo de caché** → Procede a API de DeepSeek
4. **Consulta a DeepSeek**: Envía prompt específico por idioma con resumen de datos
5. **Almacenamiento de Insight**: Guarda resultado con clave hash para ejecuciones futuras
6. **Aislamiento de Caché**: Archivos de caché separados por semana e idioma
   - `youtube_charts_2026-W14_en.json`
   - `youtube_charts_2026-W14_es.json`

### **Diagrama 3: Pipeline de Generación de Notebook**

<img src="https://raw.githubusercontent.com/adroguetth/Music-Charts-Intelligence/main/Documentation_ES/Diagramas/4_1.weekly_charts_notebook_generator/ES-Diagram%203.png" alt="Diagrama 3: Pipeline de Generación de Notebook" width="600">

Este diagrama muestra cómo se **construye el notebook Jupyter**:

1. **Carga de Plantillas**: Títulos y estructura de secciones definidos en `get_section_titles()`
2. **Creación de Celdas**: Para cada sección, crea celdas markdown y de código:
   - **Celdas markdown**: Encabezados de sección e insights de IA
   - **Celdas de código**: Código Python para análisis y visualizaciones
3. **Inyección de Contenido Dinámico**:
   - Ruta de la base de datos (ruta relativa desde notebook hasta raíz del repo)
   - Información de semana/año
   - Insights de IA (desde caché o API)
4. **Ensamblaje con nbformat**: Construye notebook usando estructuras de `nbformat.v4`
5. **Escritura de Archivo**: Guarda como `.ipynb` con metadatos apropiados (kernel Python 3)

### **Diagrama 4: Suite de Visualizaciones**

<img src="https://raw.githubusercontent.com/adroguetth/Music-Charts-Intelligence/main/Documentation_ES/Diagramas/4_1.weekly_charts_notebook_generator/ES-Diagram%204.png" alt="Diagrama 4: Suite de Visualizaciones" width="600">

El notebook incluye **más de 25 visualizaciones estáticas**:

| Tipo | Cantidad | Ejemplos |
|:-----|:---------|:---------|
| **Gráficos de Barras (horizontales)** | 5 | Top países por canciones, top países por likes, engagement por género, tipos de canal |
| **Gráficos de Barras (verticales)** | 3 | Distribución de lanzamientos, vistas por tipo de video, engagement por tipo de video |
| **Gráficos de Líneas** | 2 | Evolución de vistas por trimestre, evolución de engagement por trimestre |
| **Gráficos Circulares** | 1 | Distribución por continente |
| **Treemap** | 1 | Distribución de géneros (usando squarify) |
| **Mapas de Calor** | 1 | Matriz país vs género |
| **Histogramas** | 1 | Distribución de duración con KDE |
| **Diagramas de Caja** | 1 | Estadísticas de duración |
| **Diagramas de Dispersión** | 2 | Vistas vs cantidad de artistas, engagement vs cantidad de artistas |
| **Tablas** | 8 | Estadísticas, top canciones, métricas de video, etc. |

Todas las visualizaciones usan **colores inspirados en YouTube**:
- Primario: `#FF0000` (rojo YouTube)
- Fondo: `#FFFFFF` y `#F9F9F9`
- Texto: `#0F0F0F` y `#606060`
- Cuadrícula: `#E5E5E5`

---

## 🔍 Análisis Detallado de `4_1.weekly_charts_notebook_generator.py`

### Estructura del Código

#### **1. Clase de Configuración**

```python
class Config:
    """Configuración para el generador de notebooks."""
    
    # Repositorio GitHub
    REPO_OWNER = "adroguetth"
    REPO_NAME = "Music-Charts-Intelligence"
    FOLDER_PATH = "charts_archive/3_enrich-chart-data"
    
    # Rutas de salida
    OUTPUT_EN_PATH = Path("Notebook_EN/weekly")
    OUTPUT_ES_PATH = Path("Notebook_ES/weekly")
    CACHE_EN_PATH = Path("Notebook_EN/weekly/cache")
    CACHE_ES_PATH = Path("Notebook_ES/weekly/cache")
    
    # API de DeepSeek
    DEEPSEEK_API_URL = "https://api.deepseek.com/v1/chat/completions"

#### **2. Sistema de Caché de Insights de IA**

```python
class AIInsightsCache:
    """
    Gestor de caché para insights generados por IA.
    
    Características:
    - Aislado por semana e idioma
    - Claves de caché basadas en huella digital de datos
    - Persistencia JSON
    """
    
    def _get_data_hash(self, df: pd.DataFrame, section: str) -> str:
        """Genera hash MD5 del subconjunto de datos relevante."""
        if section == 'general_stats':
            key_data = f"{len(df)}_{df['views'].sum()}_{df['likes'].sum()}"
        elif section == 'top_countries':
            key_data = df['artist_country'].value_counts().head(10).to_string()
        # ... hasheo específico por sección
        return hashlib.md5(key_data.encode()).hexdigest()
```

**Estructura del archivo de caché:**

```json
{
  "introduction_a1b2c3d4e5f6": "Los charts de YouTube de esta semana muestran...",
  "general_stats_f6e5d4c3b2a1": "El conjunto de datos contiene 100 canciones de...",
  "top_countries_1a2b3c4d5e6f": "Estados Unidos domina con 45 canciones..."
}
```

#### **3. Integración con API de DeepSeek**

```python
def get_ai_insight(section: str, data_summary: Dict, df: pd.DataFrame, 
                   language: str, week: str) -> str:
    """
    Consulta la API de DeepSeek para obtener insights de sección.
    
    - Prompts específicos por idioma (inglés/español)
    - Máx tokens: 600 para secciones regulares, 2000 para introducción/resumen ejecutivo
    - Temperatura: 0.7 (equilibrio creatividad/consistencia)
    - Límite de tasa: Manejado por tiempo de ejecución de GitHub Actions
    """
```

**Ejemplo de prompt para notebook en español:**

```python
# Prompt de introducción (español)
prompt = f"""
Escribe una introducción atractiva y personalizada para un informe semanal 
de charts musicales de YouTube, basada en estos datos:

- Total canciones analizadas: {total_songs}
- Países representados: {unique_countries}
- Total de vistas: {total_views:,}

La introducción debe ser persuasiva, destacar la riqueza de los datos,
y tener una extensión de 6-8 líneas en español.
"""
```

#### **4. Operaciones con Base de Datos**

```python
def load_data(db_path: Path) -> pd.DataFrame:
    """Carga y prepara datos desde base de datos SQLite."""
    conn = sqlite3.connect(db_path)
    df = pd.read_sql_query("SELECT * FROM enriched_songs", conn)
    conn.close()
    
    # Ingeniería de características
    df['upload_date'] = pd.to_datetime(df['upload_date'], errors='coerce')
    df['upload_quarter'] = df['upload_date'].dt.quarter
    df['engagement'] = np.where(df['views'] > 0, 
                                 (df['likes'] / df['views'] * 100).round(2), 
                                 0.0)
    return df
```

#### **5. Generación de Notebook con nbformat**

```python
def generate_notebook(df: pd.DataFrame, db_info: Tuple, 
                      insights: Dict[str, str], output_path: Path, 
                      language: str) -> None:
    """
    Genera notebook Jupyter usando nbformat.
    
    Proceso:
    1. Crea notebook vacío con new_notebook()
    2. Configura metadatos del kernel (Python 3)
    3. Agrega celda de título con información de semana
    4. Para cada sección:
       - Agrega celda markdown con encabezado de sección
       - Agrega celda markdown con insight de IA (si está disponible)
       - Agrega celda de código con análisis/visualización
    5. Escribe el notebook a archivo
    """
```

**Ejemplo de celda de código generada:**

```python
# Top Países por Cantidad de Canciones (barh)
top_countries = (df
    .groupby('artist_country')
    .agg(total_songs=('rank', 'count'))
    .reset_index()
    .sort_values('total_songs', ascending=False)
    .head(10))

fig, ax = plt.subplots(figsize=(10, 6))
colors = plt.cm.Reds(np.linspace(0.4, 1, len(top_countries)))[::-1]
bars = ax.barh(top_countries['artist_country'], top_countries['total_songs'],
               color=colors, edgecolor='none', height=0.65)
# ... estilos
plt.show()
```

#### **6. Resúmenes de Datos para Prompts de IA**

```python
def get_data_summaries(df: pd.DataFrame) -> Dict[str, Dict]:
    """
    Precalcula todos los resúmenes de datos necesarios para insights de IA.
    
    Retorna diccionario anidado con métricas para cada sección:
    - introduction: total_songs, unique_countries, total_views, etc.
    - top_countries: string formateado de top 10 países
    - genre_engagement: tasas de engagement por género
    - video_metrics: estadísticas de rendimiento oficial/lyric/live
    - collaborations: comparación solista vs colaboración
    - executive_summary: hallazgos clave agregados
    """
```

#### **7. Análisis de Argumentos CLI**

```python
parser = argparse.ArgumentParser(
    description='Genera notebooks de análisis semanales de charts musicales con insights de IA'
)
parser.add_argument('--week', type=str, 
                   help='Semana a procesar (formato: YYYY-WXX)')
parser.add_argument('--language', type=str, 
                   choices=['en', 'es', 'both'], default='both')
parser.add_argument('--db-path', type=str,
                   help='Ruta a un archivo de base de datos específico')
```

### **Estructura de Archivos de Salida**

```text
Notebook_EN/weekly/
├── youtube_charts_2026-W14.ipynb    # Notebook en inglés ejecutado
├── youtube_charts_2026-W13.ipynb
├── youtube_charts_2026-W12.ipynb
├── cache/
│   ├── youtube_charts_2026-W14_en.json
│   ├── youtube_charts_2026-W13_en.json
│   └── ...

Notebook_ES/weekly/
├── youtube_charts_2026-W14.ipynb    # Notebook en español ejecutado
├── youtube_charts_2026-W13.ipynb
├── youtube_charts_2026-W12.ipynb
├── cache/
│   ├── youtube_charts_2026-W14_es.json
│   ├── youtube_charts_2026-W13_es.json
│   └── ...
```

---

## ⚙️ Análisis del Workflow de GitHub Actions (`4-generate-weekly-notebooks.yml`)

### **Estructura del Workflow**

```yaml
name: 4.1- Generador Semanal de Notebooks de Charts Musicales

on:
  schedule:
    - cron: '0 15 * * 1'
  workflow_dispatch:
    inputs:
      week:
        description: 'Semana específica a procesar (formato: YYYY-WXX)'
        required: false
        type: string
      language:
        description: 'Idioma a generar'
        required: false
        default: 'both'
        type: choice
        options:
          - both
          - en
          - es

permissions:
  contents: write
```

### **Pasos del Job**

#### **1. 📥 Clonar Repositorio**

```yaml
- name: 📥 Clonar repositorio
  uses: actions/checkout@v4
  with:
    fetch-depth: 0  # Historia completa para operaciones git
```

#### **2. 🐍 Configurar Python**

```yaml
- name: 🐍 Configurar Python
  uses: actions/setup-python@v5
  with:
    python-version: '3.12'
    cache: 'pip'  # Caché de dependencias para ejecuciones más rápidas
```

#### **3. 📦 Instalar Dependencias de Python**

```yaml
- name: 📦 Instalar dependencias de Python
  run: |
    pip install --upgrade pip
    # Librerías principales de ciencia de datos
    pip install pandas numpy matplotlib seaborn scipy requests
    # Soporte para treemap (reemplaza plotly con visualizaciones estáticas)
    pip install squarify
    # Generación y ejecución de notebooks Jupyter
    pip install jupyter nbformat nbconvert
```

**Justificación de dependencias:**

- `pandas`, `numpy`: Manipulación de datos
- `matplotlib`, `seaborn`: Visualizaciones estáticas
- `squarify`: Treemap estático (reemplaza plotly)
- `scipy`: KDE para análisis de duración
- `jupyter`, `nbformat`, `nbconvert`: Generación y ejecución de notebooks
- `requests`: Llamadas a API de DeepSeek

#### **4. 🔍 Determinar Semana a Procesar**

```yaml
- name: 🔍 Determinar semana a procesar
  id: get_week
  run: |
    if [ -n "${{ github.event.inputs.week }}" ]; then
      echo "week=${{ github.event.inputs.week }}" >> $GITHUB_OUTPUT
      echo "language=${{ github.event.inputs.language }}" >> $GITHUB_OUTPUT
    else
      LATEST_DB=$(ls -1 charts_archive/3_enrich-chart-data/youtube_charts_*.db 2>/dev/null | sort -r | head -1)
      if [ -z "$LATEST_DB" ]; then
        echo "❌ No se encontró base de datos en charts_archive"
        exit 1
      fi
      WEEK=$(basename "$LATEST_DB" | sed 's/youtube_charts_\(.*\)_enriched.db/\1/')
      echo "week=$WEEK" >> $GITHUB_OUTPUT
      echo "language=both" >> $GITHUB_OUTPUT
    fi
```

#### **5. 🏗️ Generar Notebooks (Solo Código)**

```yaml
- name: 🏗️ Generar notebooks (solo código)
  run: |
    python scripts/4_1.weekly_charts_notebook_generator.py \
      --week ${{ steps.get_week.outputs.week }} \
      --language ${{ steps.get_week.outputs.language }}
```

#### **6. ⚡ Ejecutar Notebooks para Incrustar Salidas**

```yaml
- name: ⚡ Ejecutar notebooks para incrustar salidas
  run: |
    cd ${{ github.workspace }}
    
    # Ejecutar notebook en inglés
    EN_NOTEBOOK="Notebook_EN/weekly/youtube_charts_${{ steps.get_week.outputs.week }}.ipynb"
    if [ -f "$EN_NOTEBOOK" ]; then
      echo "🇬🇧 Ejecutando notebook en inglés..."
      jupyter nbconvert --to notebook --execute \
        "$EN_NOTEBOOK" \
        --output "$(basename "$EN_NOTEBOOK")" \
        --output-dir Notebook_EN/weekly/
    fi
    
    # Ejecutar notebook en español
    ES_NOTEBOOK="Notebook_ES/weekly/youtube_charts_${{ steps.get_week.outputs.week }}.ipynb"
    if [ -f "$ES_NOTEBOOK" ]; then
      echo "🇪🇸 Ejecutando notebook en español..."
      jupyter nbconvert --to notebook --execute \
        "$ES_NOTEBOOK" \
        --output "$(basename "$ES_NOTEBOOK")" \
        --output-dir Notebook_ES/weekly/
    fi
```

#### **7. 🧹 Mantener Solo los Últimos 6 Notebooks Localmente**

```yaml
- name: 🧹 Mantener solo los últimos 6 notebooks localmente
  run: |
    MAX_KEEP=6
    KEEP_PLUS_ONE=$((MAX_KEEP + 1))
    
    # Notebooks en inglés
    cd Notebook_EN/weekly
    ls -1 *.ipynb 2>/dev/null | sort -r | tail -n +${KEEP_PLUS_ONE} | xargs -r rm -f
    cd ../..
    
    # Notebooks en español
    cd Notebook_ES/weekly
    ls -1 *.ipynb 2>/dev/null | sort -r | tail -n +${KEEP_PLUS_ONE} | xargs -r rm -f
    cd ../..
    
    # Archivos de caché (mantener últimos 6 por idioma)
    mkdir -p Notebook_EN/weekly/cache
    cd Notebook_EN/weekly/cache
    ls -1 *.json 2>/dev/null | sort -r | tail -n +${KEEP_PLUS_ONE} | xargs -r rm -f
    cd ../../../..
    
    mkdir -p Notebook_ES/weekly/cache
    cd Notebook_ES/weekly/cache
    ls -1 *.json 2>/dev/null | sort -r | tail -n +${KEEP_PLUS_ONE} | xargs -r rm -f
    cd ../../../..
    
    echo "🧹 Limpieza completada: Manteniendo solo los últimos ${MAX_KEEP} notebooks por idioma"
```

**Comportamiento de la Ventana Deslizante:**

| Ejecución | Notebooks | Acción                                                |
| :-------- | :-------- | :---------------------------------------------------- |
| 1ª-6ª     | 1-6       | Ninguna (por debajo del límite)                       |
| 7ª        | 7         | Elimina el más antiguo (mantiene los 6 más recientes) |
| 8ª        | 7         | Elimina el más antiguo (mantiene los 6 más recientes) |

#### **8. 💾 Commit y Push de Cambios**

```yaml
- name: 💾 Commit y push de cambios
  run: |
    git config user.name "github-actions[bot]"
    git config user.email "github-actions[bot]@users.noreply.github.com"
    
    git add Notebook_EN/weekly/ Notebook_ES/weekly/
    
    if git diff --staged --quiet; then
      echo "📭 No hay cambios para commit"
    else
      git commit -m "📊 Análisis semanal de charts - ${{ steps.get_week.outputs.week }} [skip ci]"
      git push
    fi
```

#### **9. 📊 Resumen**

```yaml
- name: 📊 Resumen
  run: |
    echo "=========================================="
    echo "🎉 ¡Análisis Semanal de Charts Completado! 🎉"
    echo "=========================================="
    echo "📅 Semana: ${{ steps.get_week.outputs.week }}"
    echo "🌐 Idioma: ${{ steps.get_week.outputs.language }}"
    echo ""
    echo "📁 Archivos generados (con salidas):"
    echo "  🇬🇧 Notebooks en Notebook_EN/weekly/"
    echo "  🇪🇸 Notebooks en Notebook_ES/weekly/"
    echo "  🗑️ Solo se mantienen localmente los últimos 6 notebooks"
    echo "=========================================="
```

### **Programación Cron**

```cron
'0 15 * * 1'  # Minuto 0, Hora 15, Cualquier día del mes, Cualquier mes, Lunes
```

### **Línea de Tiempo del Flujo de Ejecución (Lunes)**

```text
12:00 UTC ─→ Script 1: Descarga de charts
    ↓
13:00 UTC ─→ Script 2: Enriquecimiento de artistas
    ↓
14:00 UTC ─→ Script 3: Enriquecimiento de charts
    ↓
15:00 UTC ─→ Script 4_1: Generación de notebooks (ESTE SCRIPT)
    ↓
15:10 UTC ─→ Notebooks ejecutados y commiteados
```

---

## 🔐 Secretos Requeridos

| Secreto            | Propósito                                                    |
| :----------------- | :----------------------------------------------------------- |
| `DEEPSEEK_API_KEY` | Usado por la API de DeepSeek para generar insights de IA para cada sección. Requerido para funcionalidad completa; sin él, los insights muestran "Insights de IA no disponibles (clave API no configurada)". |

------

## 🚀 Instalación y Configuración Local

### **Requisitos Previos**

- Python 3.12 o superior
- Git instalado
- Clave API de DeepSeek (para insights de IA)
- Base de datos enriquecida del Script 3

### **Instalación Paso a Paso**

1. **Clonar el Repositorio**

```bash
# Clonar el repositorio principal
git clone https://github.com/adroguetth/Music-Charts-Intelligence.git
cd Music-Charts-Intelligence

# (Opcional) Si solo necesitas los scripts, puedes clonar con profundidad 1:
# git clone --depth 1 https://github.com/adroguetth/Music-Charts-Intelligence.git

# Verificar que el script existe
ls -la scripts/4_1.weekly_charts_notebook_generator.py
```

2. **Crear Entorno Virtual (recomendado)**

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

3. **Instalar Dependencias**

```bash
pip install -r requirements.txt
```

O instalar dependencias mínimas para este script:

```bash
pip install pandas numpy matplotlib seaborn squarify scipy requests
pip install jupyter nbformat nbconvert
```

4. **Configurar Clave API de DeepSeek**

```bash
# Linux/Mac
export DEEPSEEK_API_KEY="tu-clave-api-aquí"

# Windows (Command Prompt)
set DEEPSEEK_API_KEY=tu-clave-api-aquí

# Windows (PowerShell)
$env:DEEPSEEK_API_KEY="tu-clave-api-aquí"
```

5. **Ejecutar con la Base de Datos Más Reciente**

```bash
python scripts/4_1.weekly_charts_notebook_generator.py
```

6. **Ejecutar con una Semana Específica**

```bash
python scripts/4_1.weekly_charts_notebook_generator.py --week 2026-W14
```

7. **Ejecutar Solo para un Idioma Específico**

```bash
python scripts/4_1.weekly_charts_notebook_generator.py --language es
```

---

### **Configuración de Desarrollo**

```bash
# Para usar un archivo de base de datos específico
python scripts/4_1.weekly_charts_notebook_generator.py --db-path /ruta/a/base/datos.db

# Para limpiar caché (forzar nuevos insights de IA)
rm -rf Notebook_EN/weekly/cache/*.json
rm -rf Notebook_ES/weekly/cache/*.json

# Para simular el entorno de GitHub Actions
export GITHUB_ACTIONS=true
```

---

## 📁 Estructura de Archivos Generados

```text
Music-Charts-Intelligence/
├── charts_archive/
│   └── 3_enrich-chart-data/
│       └── youtube_charts_2026-W14_enriched.db   # Entrada
├── Notebook_EN/                                   # Salida en inglés
│   └── weekly/
│       ├── youtube_charts_2026-W14.ipynb         # Notebook ejecutado
│       ├── youtube_charts_2026-W13.ipynb
│       ├── youtube_charts_2026-W12.ipynb
│       └── cache/
│           ├── youtube_charts_2026-W14_en.json   # Caché de insights de IA
│           ├── youtube_charts_2026-W13_en.json
│           └── ...
├── Notebook_ES/                                   # Salida en español
│   └── weekly/
│       ├── youtube_charts_2026-W14.ipynb
│       ├── youtube_charts_2026-W13.ipynb
│       ├── youtube_charts_2026-W12.ipynb
│       └── cache/
│           ├── youtube_charts_2026-W14_es.json
│           ├── youtube_charts_2026-W13_es.json
│           └── ...
└── scripts/
    └── 4_1.weekly_charts_notebook_generator.py   # Este script
```

### **Estimaciones de Tamaño de Notebook**

| Componente                       | Tamaño (aprox.) |
| :------------------------------- | :-------------- |
| Notebook en bruto (solo código)  | 50-80 KB        |
| Notebook ejecutado (con salidas) | 500 KB - 2 MB   |
| Archivo de caché (14 secciones)  | 10-20 KB        |
| **Total semanal (EN + ES)**      | **1-4 MB**      |

------

## 🔧 Personalización y Configuración

### **Parámetros Ajustables en el Script**

```python
# En 4_1.weekly_charts_notebook_generator.py

# Máx tokens para insights de IA
if section in ["introduction", "executive_summary"]:
    max_tokens = 2000  # Aumentado para resumen de 30 líneas
else:
    max_tokens = 600

# Temperatura para creatividad de IA
"temperature": 0.7  # Rango: 0.0 (determinista) a 1.0 (creativo)

# Timeout de API
timeout=30  # segundos
```

### **Configuración del Workflow**

```yaml
# En 4-generate-weekly-notebooks.yml

timeout-minutes: 20        # Timeout total del job
MAX_KEEP: 6                # Número de notebooks a retener
```

### **Estilos de Visualización**

```python
# Colores inspirados en YouTube
YT_RED = '#FF0000'
YT_RED_DARK = '#CC0000'
YT_BG = '#FFFFFF'
YT_SURFACE = '#F9F9F9'
YT_TEXT = '#0F0F0F'
YT_GRAY = '#606060'
YT_GRID = '#E5E5E5'
```

### **Agregar Nuevas Secciones de Análisis**

1. Agregar título de sección en `get_section_titles()`
2. Agregar prompt en `get_ai_insight()` (inglés y español)
3. Agregar resumen de datos en `get_data_summaries()`
4. Agregar celda de código en `generate_notebook()`
5. Agregar a la lista `sections` en la ejecución principal

### **Modificar la Retención de Caché**

```python
# En get_data_summaries(), ajustar la generación de hash
def _get_data_hash(self, df: pd.DataFrame, section: str) -> str:
    # Agregar más columnas al hash para una invalidación de caché más granular
    if section == 'general_stats':
        key_data = f"{len(df)}_{df['views'].sum()}_{df['likes'].sum()}_{df['comments'].sum()}"
```

---

## 🐛 Solución de Problemas

### **Problemas Comunes y Soluciones**

1. **Error: "No se encontró base de datos en charts_archive"**
   - Ejecutar el Script 3 primero para generar la base de datos enriquecida
   - Verificar si existe `charts_archive/3_enrich-chart-data/`
   - Validar el patrón de nombre de la base de datos: `youtube_charts_YYYY-WXX_enriched.db`
2. **Error: "Clave API de DeepSeek no configurada"**
   - Configurar variable de entorno `DEEPSEEK_API_KEY`
   - Para GitHub Actions, agregar el secreto al repositorio
   - El script continúa sin insights de IA (muestra texto de marcador de posición)
3. **Error: "ModuleNotFoundError: No module named 'squarify'"**
   - Instalar squarify: `pip install squarify`
   - Actualizar dependencias del workflow
4. **Error: La ejecución del notebook falla con errores de Plotly**
   - Asegurar que se usa el script actualizado sin Plotly
   - Todas las visualizaciones deben ser estáticas (matplotlib/seaborn/squarify)
5. **Error: El caché no funciona entre ejecuciones**
   - Verificar permisos del directorio de caché
   - Validar el nombre del archivo de caché: `youtube_charts_{semana}_{idioma}.json`
   - Las claves de caché incluyen el hash de datos, por lo que cambios en los datos invalidan el caché
6. **Error: Timeout de GitHub Actions**
   - Aumentar `timeout-minutes` en el workflow
   - Reducir secciones u optimizar llamadas a API
   - El caché debería acelerar ejecuciones posteriores

### **Registros y Depuración**

**Habilitar salida verbosa:**

```bash
# Ejecución local con depuración
python scripts/4_1.weekly_charts_notebook_generator.py --week 2026-W14

# Verificar contenido del caché
cat Notebook_EN/weekly/cache/youtube_charts_2026-W14_en.json | jq .

# Validar notebook
jupyter nbconvert --to notebook --execute Notebook_EN/weekly/youtube_charts_2026-W14.ipynb --output test.ipynb
```

**Modo de depuración de GitHub Actions:**

```yaml
# Agregar al paso del workflow
- name: Depuración - Listar archivos
  run: |
    ls -la charts_archive/3_enrich-chart-data/
    ls -la Notebook_EN/weekly/
    ls -la Notebook_ES/weekly/
```

## 📈 Monitoreo y Mantenimiento

### **Indicadores de Salud**

| Métrica                              | Rango Esperado           | Monitoreo                                       |
| :----------------------------------- | :----------------------- | :---------------------------------------------- |
| **Tiempo de generación de notebook** | 30-90 segundos           | Duración de GitHub Actions                      |
| **Tiempo de ejecución de notebook**  | 60-120 segundos          | Tiempo de ejecución de nbconvert                |
| **Tasa de acierto de caché**         | 30-70%                   | Verificar marcas de tiempo de archivos de caché |
| **Tasa de éxito de API**             | >95%                     | Códigos de respuesta de DeepSeek                |
| **Tamaño de base de datos**          | 100-200 canciones/semana | Tamaño de archivo `.db`                         |
| **Tamaño de notebook**               | 500KB-2MB por notebook   | Tamaño de archivo `.ipynb`                      |

### **Métricas de Rendimiento**

| Fase                      | Duración (típica)        | Notas                           |
| :------------------------ | :----------------------- | :------------------------------ |
| Carga de datos            | 2-5 segundos             | Consulta SQLite + pandas        |
| Cálculo de resúmenes      | 1-2 segundos             | Operaciones de agrupación       |
| Insights de IA (en caché) | <1 segundo por sección   | Acierto de caché                |
| Insights de IA (nuevos)   | 2-4 segundos por sección | Llamada a API de DeepSeek       |
| Generación de notebook    | 5-10 segundos            | Creación de celdas con nbformat |
| Ejecución de notebook     | 30-60 segundos           | Ejecución de nbconvert          |
| **Total (en caché)**      | **~1-2 minutos**         | La mayoría de las ejecuciones   |
| **Total (semana nueva)**  | **~3-5 minutos**         | Primera ejecución de la semana  |

### **Análisis de Costos (API de DeepSeek)**

| Sección                     | Tokens (aprox.)   | Costo por ejecución |
| :-------------------------- | :---------------- | :------------------ |
| Introducción                | 800-1200          | $0.0016             |
| Estadísticas generales      | 400-600           | $0.0008             |
| Top países                  | 400-600           | $0.0008             |
| Top likes                   | 400-600           | $0.0008             |
| Engagement por género       | 400-600           | $0.0008             |
| Métricas de video           | 400-600           | $0.0008             |
| Engagement por tipo         | 400-600           | $0.0008             |
| Top canciones (3 secciones) | 1200-1800         | $0.0024             |
| Temporal                    | 400-600           | $0.0008             |
| Duración                    | 400-600           | $0.0008             |
| Colaboraciones              | 400-600           | $0.0008             |
| Resumen ejecutivo           | 1500-2000         | $0.0025             |
| **Total por semana**        | **~7,000-10,000** | **~$0.015**         |
| **Mensual (4 semanas)**     | **28,000-40,000** | **~$0.06**          |

*Costos basados en precios de DeepSeek: $0.14 por 1M tokens de entrada, $0.28 por 1M tokens de salida*

------

## 📄 Licencia y Atribución

- **Licencia**: MIT
- **Autor**: Alfonso Droguett
  - 🔗 **LinkedIn:** [Alfonso Droguett](https://www.linkedin.com/in/adroguetth/)
  - 🌐 **Portafolio web:** [adroguett-portfolio.cl](https://www.adroguett-portfolio.cl/)
  - 📧 **Email:** adroguett.consultor@gmail.com
- **Fuentes de Datos**:
  - YouTube Charts (datos originales)
  - API de DeepSeek (insights generados por IA)
- **Tecnologías**:
  - Jupyter Notebooks
  - Matplotlib, Seaborn, Squarify (visualizaciones)
  - Pandas, NumPy (procesamiento de datos)

------

## 🤝 Contribución

1. Reportar problemas con registros completos y salidas de notebooks
2. Proponer nuevas secciones de análisis con visualizaciones de ejemplo
3. Sugerir mejoras a los prompts de IA para mejores insights
4. Agregar nuevas visualizaciones usando matplotlib/seaborn
5. Mejorar la eficiencia del caché o agregar nuevas estrategias de invalidación
6. Optimizar el tiempo de ejecución de notebooks
7. Mantener compatibilidad con el esquema de base de datos enriquecida

------

## 🧪 Limitaciones Conocidas

- **Visualizaciones Estáticas**: Sin gráficos interactivos de plotly (por diseño, para compatibilidad con GitHub)
- **Dependencia de API**: Requiere clave API de DeepSeek para insights de IA
- **Enfoque Semanal**: Diseñado para charts semanales, no para análisis en tiempo real
- **Esquema de Base de Datos**: Fuertemente acoplado con el esquema enriquecido del Script 3
- **Soporte de Idiomas**: Solo inglés y español (otros idiomas requerirían nuevos prompts)
- **Tamaño del Caché**: Los archivos JSON de caché persisten indefinidamente (limpieza manual vía workflow)

---

**⭐ Si este proyecto te resulta útil, ¡considera darle una estrella en GitHub!**
