# ===============================================
# Python Dependencies for YouTube Charts Intelligence
# ===============================================

# ---------- Script 1: Download Charts ----------
# Playwright: Browser automation for downloading YouTube Charts
playwright==1.48.0

# ---------- Script 2: Artist Enrichment ----------
# Requests: HTTP calls to MusicBrainz, Wikipedia, Wikidata
requests>=2.28.0
# Pandas: Data manipulation and analysis
pandas==2.2.3
# NumPy: Numerical operations (pandas dependency)
numpy==2.1.3
# OpenAI: DeepSeek API integration (fallback for missing artist data)
openai>=1.0.0

# ---------- Script 3: Chart Enrichment ----------
# yt-dlp: YouTube metadata extraction (fallback method)
yt-dlp>=2023.10.13
# Google API Client: YouTube Data API v3 integration
google-api-python-client>=2.86.0
# isodate: ISO 8601 duration parsing (YouTube API)
isodate>=0.6.1
# Selenium: Headless browser fallback for metadata
selenium>=4.15.0
# WebDriver Manager: Automatic ChromeDriver management
webdriver-manager>=4.0.1

# ---------- Script 4: Notebook Generator ----------
# Jupyter: Notebook generation and conversion
jupyter>=1.0.0
nbconvert>=7.16.0
nbformat>=5.10.0
# Visualization (static-only, no JavaScript required)
matplotlib>=3.7.0
seaborn>=0.13.0
squarify>=0.4.3          # Static treemap (replaces plotly)
# Statistical analysis
scipy>=1.11.0
# Google Drive upload (optional, for PDF export)
google-api-python-client>=2.86.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=1.0.0

# ---------- Removed dependencies ----------
# plotly>=5.18.0  # REMOVED: Replaced by squarify for static treemaps

# ---------- Built-in Modules (NOT required) ----------
# These come with Python, no need to install:
# - sqlite3     → Database operations
# - re          → Regular expressions
# - datetime    → Date/time handling
# - pathlib     → Cross-platform paths
# - tempfile    → Temporary files
# - os, sys     → System operations
# - time        → Delays and sleep
# - collections → Counter, defaultdict
# - csv         → CSV parsing (legacy support)
# - subprocess  → Installing missing dependencies
# - json        → JSON parsing (DeepSeek responses)
# - hashlib     → Cache key generation
# - argparse    → CLI argument parsing
